{% extends 'base.html.twig' %}

{% block title %}{{ 'passwordSet.page_title' | trans }}{% endblock %}

{% block content %}
    <div class="jumbotron">
        <h1 class="display-4">{{ 'passwordSet.password_set' | trans }}</h1>
        <p class="lead">{{ 'passwordSet.your_password_was_reset' | trans }}</p>
        <hr class="my-4">
        <a class="btn btn-primary btn-lg" href="{{ path('login') }}" role="button">{{ 'passwordSet.go_to_login' | trans }}</a>
    </div>
{% endblock %}